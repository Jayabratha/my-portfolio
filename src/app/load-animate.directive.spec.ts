import { LoadAnimateDirective } from './load-animate.directive';

describe('LoadAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
